name: 'main build status'

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - name: Checkout files
      uses: actions/checkout@v2
      with:
        fetch-depth: 0
    - uses: './.github/actions/build-only'